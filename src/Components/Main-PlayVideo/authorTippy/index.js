import classNames from 'classnames/bind'

import styles from './authorTippy.module.scss'

const cx = classNames.bind(styles)

function AuthorVideo() {
    return <div></div>
}

export default AuthorVideo
